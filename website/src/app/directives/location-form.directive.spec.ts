import { LocationFormDirective } from './location-form.directive';

describe('LocationFormDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationFormDirective();
    expect(directive).toBeTruthy();
  });
});
